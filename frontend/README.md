# Radar Políticos — frontend estático (Next.js)

Site pré-compilado a partir dos JSONs de `public/dados/` (gerados por `python -m radar.exportar`).

```bash
npm install
npm run build   # gera out/ (1.547 perfis + home + rankings)
npx serve out   # ver localmente
```
